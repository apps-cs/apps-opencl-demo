## OpenCL Examples

Examples for GPU programming using OpenCL in OS Linux for the course APPS.<br>
Petr Olivka, Department of Computer Science, FEECS, VSB-TU Ostrava

---

Each directory `ocl_*` contains `Makefile` 
so call the command `make` to build program in all of them.


